stages:
  test-dvclive:
    cmd: python test_live_metrics.py
    params:
      - epochs
      - param_to_trigger_exp
    plots:
      - dvclive/plots:
          cache: false
    metrics:
      - dvclive/metrics.json:
          cache: false
